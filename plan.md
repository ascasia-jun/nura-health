# AI-DLC 서비스 개발 계획서 (plan.md)

이 문서는 Nura Health 서비스를 정적 데모에서 동적 AI 서비스로 전환하기 위한 단계별 개발 로드맵을 정의합니다.

## Phase 1: 백엔드 및 데이터베이스 기반 구축
*   **목표:** AI 모델 연동 및 사용자 데이터 처리를 위한 서버 환경을 구축합니다.
*   **주요 작업:**
    1.  백엔드 프레임워크 선정 및 기본 설정 (Node.js/Express 또는 Python/FastAPI).
    2.  AI 모델(Gemini API) 호출을 위한 서비스 모듈 구현.
    3.  사용자 프로젝트 정보, 분석 결과 등을 저장할 데이터베이스 스키마 설계 및 구축.
    4.  기본적인 API 엔드포인트 설계 (예: `/api/diagnose`, `/api/metrics`).

## Phase 2: 핵심 기능의 동적 전환 (대시보드)
*   **목표:** 기존의 모의 대시보드를 실제 데이터와 연동 가능한 구조로 변경합니다.
*   **주요 작업:**
    1.  `Features.tsx` 컴포넌트 리팩토링: 정적 애니메이션을 제거하고 백엔드 API로부터 데이터를 받아 표시할 수 있도록 수정.
    2.  '데이터 스트리머' 및 'AI 샘플러'가 API를 통해 (초기에는 모의) 데이터를 주기적으로 요청하고 화면을 갱신하는 로직 구현.
    3.  소스코드 저장소(Github) 연동을 위한 기초 UI 및 인증(OAuth) 플로우 구현.

## Phase 3: 핵심 기능의 동적 전환 (AI 진단/처방)
*   **목표:** 정적인 프로토콜 소개를 동적인 AI 진단 시스템으로 전환합니다.
*   **주요 작업:**
    1.  `ProtocolGeneratorWizard.tsx` 컴포넌트 신규 생성: 사용자가 프로젝트의 문제점을 입력할 수 있는 UI 구현.
    2.  `Protocols.tsx` 컴포넌트 수정: 기존의 정적 카드 대신, Wizard 컴포넌트를 통해 AI 진단 프로세스를 시작하는 입구 역할로 변경.
    3.  Wizard에서 입력받은 내용을 백엔드 `/api/diagnose`로 전송하고, AI가 생성한 맞춤형 프로토콜 결과를 받아와 화면에 렌더링하는 기능 구현.

## Phase 4: 신규 기능 개발 (AI 어시스턴트)
*   **목표:** 사용자 편의성을 극대화하기 위한 대화형 AI 어시스턴트를 도입합니다.
*   **주요 작업:**
    1.  `AICompanionChat.tsx` 컴포넌트 신규 생성: 플로팅 챗봇 UI 구현.
    2.  사용자 질문을 컨텍스트와 함께 백엔드 API로 전송하고, 답변을 받아 표시하는 기능 구현.
    3.  답변 내용에 따라 특정 기능(예: 프로토콜 생성)을 제안하거나 관련 페이지로 안내하는 상호작용 로직 추가.

## Phase 5: 멤버십 연동 및 전체 통합
*   **목표:** 개발된 동적 기능들을 멤버십 등급과 연동하고 전체 시스템을 통합합니다.
*   **주요 작업:**
    1.  사용자 인증 및 로그인/로그아웃 기능 구현.
    2.  `Membership.tsx` 컴포넌트 수정: 사용자의 멤버십 등급에 따라 '실시간 대시보드', 'AI 진단' 등의 기능 접근 권한을 제어하는 로직 적용.
    3.  전체 UI/UX 흐름을 점검하고, 디자인 일관성을 유지하며 최종 완성.
